services:
  vietjet-token-server:
    build: .
    ports:
      - "3001:3001"
    environment:
      PORT: 3001
      TOKEN_SERVICE_SECRET: ${TOKEN_SERVICE_SECRET:-dev-secret}
    restart: unless-stopped
    # Playwright needs a shared memory segment larger than the Docker default
    shm_size: "256mb"
